let numero = parseint(prompt("escreva um numero?"));
if (numero>0) {
     console.log(`voce escreveu um numero positivo. Seu numero: ${numero} `);
} else if(numero<0) {
     console.log(`voce escreveu um numero negativo. Seu numero: ${numero} `);
}else{
    console.log(`voce escreveu o numero zero. seu numero : ${numero} ` )
}
