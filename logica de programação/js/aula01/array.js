let nomes = ["joao pedro", "lucas", "jonathan", "joao gomes"];
let vazio = [];
let numeros = [1, 2, 3, 4, 5, 6];
let misto = [1.1, true, "a"];

console.table(misto);

carros = ["gol, pao"];

for (let i = 0; i <= numeros.length; i++) {
	if (numeros[i] % 2 == 0) {
		console.table(numeros[i]);
	}
}
nomes.sort()
console.log(nomes)

